name: build and test

on:
  push:
    branches: [ dev , latest ]
  pull_request:
    branches: [ dev , latest ]

jobs:
  test:
    runs-on: ubuntu-latest
    container: 
      image: akrck02/gtd-lib-test:v1
    steps:
    - uses: actions/checkout@v2
      with: 
        path: "GTD-LIB-TS"
    - name: RunTest
      run: | 
        ls -lha GTD-LIB-TS

